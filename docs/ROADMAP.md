# 接下来做什么

> 这份只记「还没做的」和「为什么按这个顺序」。做完的东西看 git log ——
> 每个 commit message 里写了当时的判断依据，比事后补的总结准。

---

## 两个待你拍板的（不做就上不了线）

### 1. 模型自托管：文件放哪

现在 `NEXT_PUBLIC_WASM_BASE` / `SEG_MODEL` / `FACE_MODEL` / `HAND_MODEL` 全指向
jsdelivr 和 storage.googleapis.com。**国内大概率加载不出来**，用户看到的是页面卡在
「Loading the person-segmentation model…」——不报错，就是一直转。

| 方案 | 需要什么 | 说明 |
|---|---|---|
| Vercel Blob | `BLOB_READ_WRITE_TOKEN` | 和部署同一家，最省事，几十 MB 在免费额度内 |
| Cloudflare R2 | account id + bucket + API token | README 推荐的，国内访问比 Vercel 好 |
| 塞进 `public/` | 不需要凭据 | 最快能验证链路，但每次部署传十几 MB，仓库变大 |

⚠️ **别把这几个变量直接填成 `/vendor/...`**：`public/vendor` 在 `.gitignore` 里，
产物里没有这些文件，那样会让 `/vendor/*` 全部 404，比现在还糟。README 特意标了这个坑。

### 2. iOS 真机验证

生产 URL 已经有了，拿手机开一下就能验这三条（`PRD` 7.2 把 iOS Safari 标成最大风险，
这个判断是对的）：

1. **能不能录出 mp4。** 录制按钮上写着实际拿到的容器格式。这条不通，产品对 Reels
   创作者就不成立。
2. **移动端帧率。** 引擎的降级触发条件是 `pointer: coarse`，这个启发式很粗糙。
   `signal-lost` 和 `finger-flowers` 是目前最重的两个（各跑两个模型），先拿它们试。
3. **微信内置浏览器。** iOS 微信的 `getUserMedia` 限制多，现在既没检测也没引导。

桌面 Chrome 测不出这三条 —— iOS 上所有浏览器内核都是 WebKit。

---

## ③ 泡泡模拟器 —— 做了（`soap-bubbles`）

指尖发射改成了「一开场满屏浮着 + 十根指尖划过去戳破 + 破了不再生」，其余照原计划：
instanced quad、按 y 排序的半透明合成、折射源视频、边缘薄膜干涉。

**原文说「唯一有设计风险的部分是可变状态」，这个判断偏保守了。** 风险来自
「一池会互相影响的对象怎么可回放」，而真做下来位置可以写成**闭式**：

    y(t) = y0 + vy * (t - t0)
    x(t) = x0 + sin(t * 0.21Hz + phase) * wobble

于是一个泡泡从生到死只存几个不变量，每帧不修改任何东西 —— 没有逐帧积分就
没有「浮点求和顺序敏感」。真正可变的只剩一个**单调递减的集合**（破了不再生），比 append-only 还简单。
不再生也是这个玩具和屏保的区别 —— 全部戳完屏幕就空了，它有终局。

**代价是没做泡泡之间的碰撞**：参考素材里它们本来就是互相穿过的，做碰撞要
放弃闭式位置、回到逐帧积分，把上面那一整段简单性都赔进去。要做也该等到它
真的成为观感瓶颈。

原文的 d（`controls` 对非粒子模板打通 + 离散控件）**也做了**：
新增 `element.<元素 id>.<参数名>` 这种 target，`Control.options` 支持离散档位
（Mild / Medium / Strong / Extreme 这类）。能挂哪些参数见 `engine/tunables.ts` ——
那是引擎和校验器共用的**唯一名单**，两边各写各的正是这个仓库反复出问题的地方。

## 状态层一通就顺带解锁的

`stepTo` + 定步长 + 固定时间网格采样这套地基（见 `src/engine/trail.ts` 的注释）
不是为某一个模板付的钱。`EFFECT-QUALITY` 里这两条现在都变便宜了：

- **P-2 积雪累积**：雪落到肩膀上盖一层，慢慢变厚，人一动就掉。
  原文写的「设计要先解决这个再动手」指的就是无历史性这堵墙。
- **P-8 液体湿痕**：同一套累积缓冲，换个材质渲成湿痕。

两条都要**累积缓冲**（一张跨帧的贴图），比轨迹的环形缓冲重，但纪律是同一套。

---

## 验证上还欠的

**冒烟验不到「弯手指长茎」。** `smoke:live` 的假摄像头喂的是一张**伸开的手**的照片
（做成 Y4M 循环播放），真模型在上面读出来的弯曲度是 0.00~0.01 —— 也就是说
`finger-flowers` 在冒烟里跑得通、不报错、但**一根茎都不会长**。

茎这套机制目前由离线测试覆盖：`scripts/make-hand-sequence.ts` 合成了一段
弯曲扫动（用引擎自己的公式反解角度，保证真的扫过 0→1），`hand-stem` 探针模板
钉住「直接跳到 t 和逐步积到 t 逐位相同」这条纯函数性质。

要在冒烟里也覆盖，得有一段**真的在弯手指**的视频喂给假摄像头。现在没有。
在补上之前，别把 `smoke:live` 绿了当成「茎在真链路上是好的」。

## 需要 stepTo 的那一族

有跨帧状态、离线渲染必须按定步长积过去的：`trail`、`pinch-bloom`、`bubbles`、
以及 **`fluidity`**（它的节奏由运动速度驱动，而速度按定义需要两帧）。

`stem` 不在这一族 —— 它的长度由当前帧的手指弯曲度决定，是纯函数。

## 观感上还欠的

- **暗边参数没在真白墙上调过。** 雪的暗边是在离线 harness 的浅灰背景上定的
  （`src/engine/particles.ts` 里的 rim），真机对着白墙要再调一轮。
- **`crying` 的眼泪在深色皮肤上偏「蓝色贴纸」。** `screen` 混合在浅肤色上像湿光，
  在偏暗的照片上不成立。换了真人 fixture 之后才暴露出来的。
- **`X-1` 全局后期**（bloom / grain / 暗角）一条都没做。`EFFECT-QUALITY` 说它
  「一个全局 look pass 会同时抬高三个类目」，判断是对的，但要计入移动端的降级档位。
